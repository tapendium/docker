steps:
  - label: "Build image"
    key: build
    command:
      - "docker build -t ${CONTAINER_NAME} -f ./${CONTAINER_NAME}/Dockerfile ."
  - label: "list images"
    depends_on: build
    command:
      - "docker image ls"
  - label: "Dockerfile unit-test container"
    depends_on: build
    plugins:
      - docker#v3.13.0:
          image: "zemanlx/container-structure-test:latest"
          propagate-environment: true
          volumes:
            - "${BUILDKITE_BUILD_CHECKOUT_PATH}:/currentdir/app"
          command:
            - "test"
            - "--image"
            - "${CONTAINER_NAME}"
            - "--config"
            - "/currentdir/app/${CONTAINER_NAME}/unit-test.yml"
